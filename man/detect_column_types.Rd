% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_column_types.R
\name{detect_column_types}
\alias{detect_column_types}
\title{Detect the EDARK type of each column in a dataset}
\usage{
detect_column_types(dataset)
}
\arguments{
\item{dataset}{A \code{data.frame} or \code{tibble}.}
}
\value{
A named character vector, one element per column, with values in
\code{c("numeric", "factor", "datetime", "character")}.
}
\description{
Returns a named character vector mapping each column name to one of four
EDARK type strings: \code{"numeric"}, \code{"factor"}, \code{"datetime"}, \code{"character"}.
This vector is stored in \code{shared_state$column_types} and drives all plot
routing and UI dropdown population throughout the app.
}
\details{
Detection rules (first match wins):
\itemize{
\item \code{POSIXct} / \code{POSIXlt} / \code{Date} → \code{"datetime"}
\item \code{factor} / \code{ordered} → \code{"factor"}
\item \code{numeric} / \code{integer} / \code{double} → \code{"numeric"}
\item Everything else → \code{"character"}
}
}
\examples{
df <- data.frame(
  age    = c(30, 45, 60),
  group  = factor(c("A", "B", "A")),
  date   = as.POSIXct(c("2024-01-01", "2024-02-01", "2024-03-01")),
  notes  = c("good", "ok", "bad"),
  stringsAsFactors = FALSE
)
detect_column_types(df)
# age → "numeric", group → "factor", date → "datetime", notes → "character"
}
