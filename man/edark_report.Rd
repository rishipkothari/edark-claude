% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edark_report.R
\name{edark_report}
\alias{edark_report}
\title{Generate an EDARK report without launching the GUI}
\usage{
edark_report(
  data,
  report_type = "all_vars",
  variables = NULL,
  primary_variable = NULL,
  primary_role = "exposure",
  stratify_variable = NULL,
  report_format = "html",
  output_path = NULL,
  max_factor_levels = 20
)
}
\arguments{
\item{data}{A \code{data.frame}.}

\item{report_type}{\code{"all_vars"} (default) for an all-variables description
report, or \code{"primary_vs_others"} for a bivariate primary-vs-all report.}

\item{variables}{Character vector of column names to include. Defaults to all
columns.}

\item{primary_variable}{For \code{report_type = "primary_vs_others"}: the name
of the primary variable.}

\item{primary_role}{\code{"exposure"} (default) or \code{"outcome"}. Controls
axis assignment for bivariate plots.}

\item{stratify_variable}{Optional column name to stratify all bivariate plots.
Only used when \code{report_type = "primary_vs_others"}.}

\item{report_format}{Output format: \code{"html"} (default), \code{"pptx"},
or \code{"docx"}.}

\item{output_path}{Path to write the report. Defaults to
\code{"edark_report.<ext>"} in the current working directory.}

\item{max_factor_levels}{Maximum unique values allowed for factor columns
before they are treated as high-cardinality. Default \code{20}.}
}
\value{
Invisibly returns \code{output_path}.
}
\description{
Programmatic API for report generation. Accepts a raw data frame, applies
the same input validation and type-casting as \code{edark()}, then writes a
report to \code{output_path}.
}
\examples{
\dontrun{
# All-variables HTML report
edark_report(liver_tx, report_format = "html",
             output_path = tempfile(fileext = ".html"))

# Primary vs others PowerPoint
edark_report(liver_tx,
             report_type      = "primary_vs_others",
             primary_variable = "age_tx",
             primary_role     = "exposure",
             report_format    = "pptx",
             output_path      = tempfile(fileext = ".pptx"))
}

}
