% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cast_column_types.R
\name{cast_column_types}
\alias{cast_column_types}
\title{Auto-cast dataset columns to appropriate types}
\usage{
cast_column_types(dataset, max_factor_levels = 20)
}
\arguments{
\item{dataset}{A \code{data.frame} or \code{tibble}.}

\item{max_factor_levels}{Integer. Character columns with no more than this
many unique non-NA values are converted to \code{factor}. Default \code{20}.}
}
\value{
A \code{data.frame} with columns cast according to the rules below:\tabular{lll}{
   Priority \tab Condition \tab Action \cr
   1 \tab \code{Date} column \tab Convert to \code{POSIXct} at midnight UTC \cr
   2 \tab \code{logical} column \tab Convert to \code{factor} with levels \code{FALSE}, \code{TRUE} \cr
   3 \tab \code{character} column where all non-NA values parse as numeric \tab Convert to \code{numeric} \cr
   4 \tab \code{character} column with ≤ \code{max_factor_levels} unique non-NA values \tab Convert to \code{factor} \cr
   5 \tab Anything else \tab Leave unchanged \cr
}
}
\description{
Applies deterministic type-casting rules to a data frame at launch time.
Rules are applied in priority order; a column is only transformed by the
first rule that matches it. The original data frame is never modified —
a new data frame is returned.
}
\examples{
df <- data.frame(
  a = as.Date("2024-01-01"),
  b = TRUE,
  c = c("1.5", "2.0", "3.1"),
  d = c("low", "high", "low"),
  e = 42L,
  stringsAsFactors = FALSE
)
cast_column_types(df)
}
