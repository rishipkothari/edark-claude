% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edark.R
\name{edark}
\alias{edark}
\title{Launch the EDARK exploratory data analysis GUI}
\usage{
edark(dataset = liver_tx, max_factor_levels = 20)
}
\arguments{
\item{dataset}{A \code{data.frame} or \code{tibble}. Defaults to the built-in
\code{liver_tx} synthetic liver transplant dataset so that \code{edark()} with no
arguments launches immediately for testing and demonstration.}

\item{max_factor_levels}{Integer. Character columns with no more than this
many unique non-NA values are auto-converted to \code{factor} at launch.
Also used as the high-cardinality guard threshold in the Explore stage.
Default \code{20}.}
}
\value{
Launches a Shiny app (does not return a value).
}
\description{
The primary entry point for the package. Validates the input dataset,
auto-casts column types, initialises the session-scoped shared state, and
launches the Shiny application.
}
\examples{
\dontrun{
edark()                  # launches with built-in liver_tx demo data
edark(mtcars)
edark(palmerpenguins::penguins, max_factor_levels = 10)
}
}
