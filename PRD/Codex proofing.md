For your proofing agent
The rules are written in two places:

CLAUDE.md, "Statistical methods registry": a table of every quantity, the function that computes it, and where it's used. It also lists the R functions (confint, chisq.test, cor.test and so on) that must not be called anywhere else.
PRD §4.2: the same rules written as the spec.
Your proofing agent can check the app against either one.